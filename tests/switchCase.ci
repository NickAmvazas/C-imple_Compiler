program checkSwitchCase
declare x,y;
#main#
{
	input(x);
	switchcase
		case(x<=0)
		{
			
			y:=x*2;
		}
		case(x>0)
		{
			print(y*3);
			y:=x*2;
		}
		case(x=2)
		{
		
		print(x+y);
		
		}
		default print(x*2);
		;
	print(x);
}.
